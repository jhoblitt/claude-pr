version: 2
updates:
  # GitHub Actions are pinned to commit SHAs with ratchet (`ratchet update`),
  # which uses a comment format Dependabot does not understand — so no
  # github-actions ecosystem here.
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
